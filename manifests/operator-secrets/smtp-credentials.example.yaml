# TEMPLATE ONLY — do not commit real values. Prefer `kubectl create secret`.
# Referenced by values: smtp.existingSecret (smtp.enabled=true)
apiVersion: v1
kind: Secret
metadata:
  name: jama-smtp
  namespace: jama
type: Opaque
stringData:
  username: "REPLACE_ME"   # OPERATOR
  password: "REPLACE_ME"   # OPERATOR
