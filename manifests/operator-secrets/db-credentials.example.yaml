# TEMPLATE ONLY — do not commit real values. Prefer `kubectl create secret`.
# Referenced by values: database.existingSecret + database.usernameKey/passwordKey
apiVersion: v1
kind: Secret
metadata:
  name: jama-db
  namespace: jama
type: Opaque
stringData:
  username: "REPLACE_ME"   # OPERATOR: external MySQL 8.0/8.4 or MS SQL Server 2022 user
  password: "REPLACE_ME"   # OPERATOR
