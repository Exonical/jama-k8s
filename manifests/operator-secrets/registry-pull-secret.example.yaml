# TEMPLATE ONLY — do not commit real values.
# Prefer: kubectl -n jama create secret docker-registry jama-registry \
#           --docker-server=REGISTRY --docker-username=USER --docker-password=PASS
# Referenced by values: global.imagePullSecrets: [ {name: jama-registry} ]
apiVersion: v1
kind: Secret
metadata:
  name: jama-registry
  namespace: jama
type: kubernetes.io/dockerconfigjson
stringData:
  # OPERATOR: replace with a real docker config JSON (base64 not required for stringData).
  .dockerconfigjson: |
    {"auths":{"REPLACE_REGISTRY":{"username":"REPLACE_ME","password":"REPLACE_ME","auth":"REPLACE_BASE64_USER:PASS"}}}
